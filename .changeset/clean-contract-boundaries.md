---
'prisma-effect-kysely': patch
---

Fix Prisma 8 domain enums, PostgreSQL driver-native codec values, namespace-safe generation, encoded Kysely table types, explicit join models, and multi-domain output.
