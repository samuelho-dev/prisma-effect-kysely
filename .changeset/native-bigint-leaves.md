---
'prisma-effect-kysely': major
---

Generate Prisma `BigInt` fields as native `bigint` values with `Schema.BigInt` across Effect codecs and Kysely table types, including nullable fields, arrays, mapped columns, and Prisma 8 contract inputs.
